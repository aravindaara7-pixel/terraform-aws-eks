apiVersion: v1
kind: Service
metadata:
  name: {{ include "terraform-aws-eks-chart.fullname" . }}
  labels:
    {{- include "terraform-aws-eks-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "terraform-aws-eks-chart.selectorLabels" . | nindent 4 }}
