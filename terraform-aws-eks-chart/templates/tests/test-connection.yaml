apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "terraform-aws-eks-chart.fullname" . }}-test-connection"
  labels:
    {{- include "terraform-aws-eks-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "terraform-aws-eks-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
